import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/admin/journal/$journalId')({
  component: RouteComponent,
})

function RouteComponent() {
  return <div>Hello "/admin/journal/$journalId"!</div>
}
